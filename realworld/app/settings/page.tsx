import React from 'react';
import {LogoutButton} from "@/projects-component/logout";

const Page = () => {
    return (
        <div>
            <LogoutButton/>
        </div>
    );
};

export default Page;